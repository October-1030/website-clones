# Storybound v1.17.0 完整复刻审计

更新时间：2026-08-02（America/Los_Angeles）

## 证据与方法

本轮以公开的 Storybound 1.17.0 安装包为当前原版证据，静态解包后从当前入口依赖图选择活动 chunk；同名旧 chunk 不计入契约。安装包没有执行。

- 安装包：`C:\tmp\Storybound_1.17.0_x64-setup.exe`（150,865,092 字节）
- 当前入口：`index-B_XgV9A-.js` + `index-Drc-tGGR.css`
- 路由、chunk、当前页面和边界总表：`docs/research/v1.17.0/ROUTE_MATRIX.md`
- 原版提示词：8 个赛道的四段提示词与 v1.16.1 哈希一致，当前库标记为 Storybound 1.17.0

验收不以“页面能打开”为完成证据。每个入口至少检查：路由挂载、分支控件、真实 API/本地结果、错误边界、构建和浏览器日志。

## 本轮结论

### 已真实验证

- 19 个路由入口全部挂载，没有本站浏览器运行错误。
- 图文任务的输入、赛道、系统提示词、三种执行模式、四种暂停策略、改写强度、叙事视角、带货/开头/尾部开关、素材来源、比例、13 种画风、动态分镜、封面、TTS 引擎/音色/语速、逐镜/连续旁白均已展开点击。
- MiniMax M3 使用原版四段提示词完成了真实 `precheck -> rewrite -> storyboard -> prompts`，得到 18 个分镜和 18 个绘图提示词。
- MiniMax TTS 词级时间戳验证通过；新建页已修复异步自动选择 MiniMax 后仍误报缺少 TTS 的状态错误。
- 历史、队列和批次总结使用真实本地任务；筛选、选择和批量执行栏已经浏览器验证。
- 草稿模板包含画布、画面区域、运镜、主标题、副标题、字幕、AI 标注、底色、音量、BGM 淡出和上下边框参数。
- HTML 动画包含文案、素材、配音、动画预览、出片、历史六阶段；逐场编辑和真实 H.264/AAC 出片通过。
- 音乐 MV 已恢复 AI 歌词/作曲与本地音乐双入口；本地音乐路线可真实生成图片、时间轴、MP4 和剪映草稿。
- 设置页八个主分区以及即梦、全能绘图、RunningHub、魔搭、自定义图片五个分支均已浏览器验证。
- 选品助手和市场的本地/导入/编辑/详情/安装路径已经验证，不虚构实时榜单、积分或交易成功。

### 自动验证

- `npm run build`：通过
- `npm run lint`：通过
- `smoke:task`：通过，8 赛道、16 文件、剪映 ZIP
- `smoke:continuous`：通过，单条连续旁白与字幕拆分
- `smoke:alignment`：通过，MiniMax 词级时间戳首尾完整
- `smoke:media`：通过，540×960、30fps、H.264/AAC、13.908 秒 MP4 与剪映 ZIP
- `smoke:benchmark`：5/5 通过
- 密钥扫描：源码中没有发现 secret-like 值；本机 MiniMax Key 只由服务端读取

## 不能诚实声称等价的私有依赖

以下是原版私有服务器或供应商能力，不属于只靠公开客户端与现有 MiniMax Key 可以复制的部分：

- 原版邮箱登录、设备、试用、激活码、积分、购买、邀请码、兑换和市场交易/发布
- 原版视频号账号抓取凭据；单视频号解析仍可用
- 当当榜实时抓取与原站榜单筛选服务
- 原作者 AI 作曲供应商；MiniMax M3/image-01/TTS 不能被无证据地冒充为歌曲生成接口
- RunningHub 动态分镜和其他需要单独账号/Token 的 Provider

这些入口保留原版层级并显示真实依赖状态，不返回假成功。像素与本地工作流可以复刻；未经授权的私有服务不能被称为“已复刻”。

## 今晚可用主路径

`图文任务 -> 粘贴/AI 创作文案 -> MiniMax M3 原版提示词 -> MiniMax image-01 -> MiniMax 1.0× TTS -> 字幕/真实时长 -> MP4/剪映草稿`

本轮状态：核心本地创作链路已达到可使用和可验证状态；私有服务边界仍按上述清单保留。
