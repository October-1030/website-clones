# 本地弹窗基础组件

本地实现组件，无对应独立原站区块。用途：承载教程、搜索、公告和明确标注的业务演示说明。

原生dialog.showModal()建立模态焦点范围，Escape与遮罩点击关闭，卸载时close()。普通弹窗白底、12px圆角、max-width calc(100vw - 32px)，最大高度calc(100dvh - 32px)，可滚动；遮罩#17203366和blur3px。标题/内容由调用方传入，不渲染任意HTML。

所有弹窗在手机尺寸受视口限制。教程使用独立全屏蓝色视觉主题。没有资产。
