# PasswordVisibilityIcon specification

Target src/components/icons.tsx. Named export PasswordVisibilityIcon. SVG viewBox0 0 1024 1024, width14px height14px currentColor #a8abb2. Mark aria-hidden. Paths extracted from visible original password-field suffix after entering dummy data. Display only when password has text. Parent button owns accessible name and toggles input type locally.
