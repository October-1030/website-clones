# Optional: configure a model on the server instead of entering a key in the dialog.
# Copy to .env.local, supply your own key, then restart the dev server.
# Never prefix the key with NEXT_PUBLIC_.
WRITING_API_KEY=
WRITING_BASE_URL=https://api.minimaxi.com/v1
WRITING_MODEL=MiniMax-M3
